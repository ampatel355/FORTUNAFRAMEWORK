# Fortuna — GitHub Repository Deliverable (Reproducibility Package)

This document specifies the public repository layout and ships the four root-level files (`README.md`, `requirements.txt`, `LICENSE`, `CITATION.cff`) for *Structure-Preserving Randomization Inference for Decision Timing* (Aryan Patel). All script names, env-var names, and output CSV names below were taken verbatim from `Code/` and the manuscript's reproducibility appendix (`supplement.tex`, §S Reproducibility).

---

## (a) Recommended repository structure

```
Fortuna/
├── Code/                  # All analysis code: SPR null, 11 strategy agents, validation & figure scripts
├── Data_Clean/            # Derived artifacts: regime-labeled OHLCV, realized trade logs, per-asset result CSVs (1d/, 1h/, 4h/)
├── Charts/                # Publication figures (PNG) regenerated by the analysis scripts
├── ProvidedFigures/       # Final numbered manuscript figures (Figure_1.png … Figure_19.png)
├── paper.tex              # Main manuscript source (assembled by Code/build_main.py)
├── supplement.tex         # Online Supplementary Appendix source (assembled by Code/build_supplement.py)
├── paper.pdf              # Compiled main paper
├── supplement.pdf         # Compiled supplement
├── requirements.txt       # Pinned Python dependencies
├── LICENSE                # MIT license (code) — see data terms in README
├── README.md              # This overview + exact per-result replication table
└── CITATION.cff           # Machine-readable citation metadata
```

One-line folder descriptions:

| Path | Contents |
|---|---|
| `Code/` | The structure-preserving randomization null (`monte_carlo.py`), the eleven strategy agents + metrics, the data loader, and every validation/figure script invoked in the reproduction table. |
| `Data_Clean/` | Canonical derived data: regime-labeled price series, realized trade logs, per-strategy Monte Carlo results, and the `*_full_comparison.csv` outputs the cross-asset panel aggregates. Subfolders `1d/`, `1h/`, `4h/` hold the frequency-robustness panels. |
| `Charts/` | Figures emitted by the analysis scripts (e.g. `competitor_comparison.png`, `cross_asset_multiplicity.png`, `mc_panel.png`). |
| `ProvidedFigures/` | The final numbered figures embedded in `paper.tex`/`supplement.tex`. |

> Note: the live repo currently also contains `fortuna_full_backup.tex` (the source `build_main.py`/`build_supplement.py` slice the released `.tex` from) and a `Code/__pycache__/` directory. Keep `fortuna_full_backup.tex` if you want the assemble step to be reproducible; add `__pycache__/`, `Data_Clean/**/*__run_*Z_pid*.csv` (per-run scratch copies), and `.rendered*/` working dirs to `.gitignore` before publishing.

---

## (b) README.md

```markdown
# Fortuna — Structure-Preserving Randomization Inference for Decision Timing

Replication code and derived data for:

> Aryan Patel. *Structure-Preserving Randomization Inference for Decision Timing.*
> Submitted to **Risks** (MDPI).
> Archived: https://github.com/ampatel355/FORTUNAFRAMEWORK · DOI: https://doi.org/10.5281/zenodo.20695539

## Overview

This repository reproduces every table and figure in the paper and its Online
Supplement. The method is **structure-preserving randomization inference**: a
conditional randomization test that holds the realized trade structure and the
exogenous price path fixed and re-randomizes only the *placement* of trades on
the calendar, from a known conditional law. It is finite-sample valid under
exchangeability of placements. The leading application is entry timing on gold
futures (`GC=F`); the headline is a null — no robust, measure-invariant
entry-placement skill — confirmed on an 11-strategy `GC=F` panel, a 322-test
cross-asset panel under multiplicity control, a measure-family sensitivity range
with an intersection–union reading, and a head-to-head against White's Reality
Check and Hansen's SPA.

The structure-preserving null is implemented in `Code/monte_carlo.py`; the eleven
strategy rules and the verdict classifier are in the `*_agent.py` /
`*_metrics.py` modules and documented in the Supplement (Section S1).

## Repository layout

| Path | Contents |
|------|----------|
| `Code/` | SPR null, strategy agents/metrics, data loader, validation & figure scripts |
| `Data_Clean/` | Derived data: regime-labeled OHLCV, realized trade logs, per-asset result CSVs (`1d/`, `1h/`, `4h/`) |
| `Charts/` | Figures emitted by the analysis scripts |
| `ProvidedFigures/` | Final numbered manuscript figures |
| `paper.tex`, `supplement.tex` | LaTeX sources |
| `paper.pdf`, `supplement.pdf` | Compiled PDFs |

## Installation

Requires **Python 3.11+** (developed and run under 3.14). No compiler or GPU
needed; all dependencies are pure-Python wheels.

```bash
git clone https://github.com/ampatel355/FORTUNAFRAMEWORK.git
cd Fortuna
python3 -m venv .venv
./.venv/bin/python -m pip install --upgrade pip
./.venv/bin/python -m pip install -r requirements.txt
```

All reproduction commands below assume the virtual environment lives at `.venv/`
and are run from the repository root. They write outputs into `Data_Clean/` and
`Charts/`; the derived inputs they consume are already committed, so **no network
access is required** to reproduce the published numbers.

## Seed protocol

Every Monte Carlo run is deterministic under a documented seed protocol.

- **Single-ticker `GC=F` panel.** Fixed initial seed **42**; independent
  strategy-specific pseudo-random streams are spawned from it. For deterministic
  strategies the realized trade log is fixed across runs and only the null
  simulations vary. The Random baseline regenerates its realized trade sequence
  under each seed (so realized outcome *and* null vary).
- **Seed-robustness (Supplement S5).** 100 outer runs with outer-run seed
  `seed_r = 100 + r`, `r = 1..100`, 5,000 simulations per run.
- **Synthetic worlds.** Base seed **9100**.
- **Competitor comparison.** Fixed world-offset seeds with `PYTHONHASHSEED=0`.
- **Positive control / signals.** One spawned stream per seed; `PC_SEEDS` /
  `PC_SIGNAL_SEEDS` control the count.

Determinism is enforced by `MONTE_CARLO_REPRODUCIBLE` / `MONTE_CARLO_SEED` and the
matching `*_REPRODUCIBLE` / `*_SEED` pairs in each harness. Several scripts
re-verify their regenerated statistics against the published values and **abort on
any material mismatch**, so the figures and the text cannot silently diverge.

## Reproducing each result

Run from the repository root. Env vars set the documented run sizes; smaller
values reproduce the same point estimates within Monte Carlo error and run faster.

### Main paper

| Result | Script | Command (env vars as published) | Output |
|--------|--------|----------------------------------|--------|
| **Table 3** — `GC=F` 11-strategy panel (`tab:main`); equity curves, MC panel, RCSI/percentile, regime ratios (S6 figures) | `Code/regenerate_gallery.py` | `./.venv/bin/python Code/regenerate_gallery.py` | figures in `Charts/`; verified against published values |
| **Table 1 / Fig 1** — competitor comparison vs RC & SPA, 5 known-truth worlds (`tab:competitor`, `fig:competitor`) | `Code/competitor_comparison.py` | `CC_REPLICATIONS=400 CC_BOOT=999 CC_SIMULATIONS=2000 PYTHONHASHSEED=0 ./.venv/bin/python Code/competitor_comparison.py` | `Data_Clean/competitor_comparison.csv`, `Charts/competitor_comparison.png` |
| **Table 4** — schedule-measure sensitivity, 3 measures (`tab:schedule_sensitivity`) | `Code/schedule_measure_sensitivity.py` | run once per measure (see below) | `Data_Clean/GC=F_sensitivity_*.csv` |

Schedule-measure sensitivity — one invocation per column:

```bash
MEASURE_LABEL=baseline \
  ./.venv/bin/python Code/schedule_measure_sensitivity.py
MONTE_CARLO_MIN_LEADING_INDEX=200 MEASURE_LABEL=leading_gap_200 \
  ./.venv/bin/python Code/schedule_measure_sensitivity.py
MONTE_CARLO_CONTEXT_MATCHING=1 MEASURE_LABEL=context_matched \
  ./.venv/bin/python Code/schedule_measure_sensitivity.py
```

The `baseline` column reproduces Table 3 exactly, confirming the sensitivity
harness matches the canonical pipeline.

### Online Supplement

| Result | Script | Command | Output |
|--------|--------|---------|--------|
| **Table S1, Figs S1–S2** — synthetic validation (size, power, KS uniformity) | `Code/synthetic_timing_experiments.py` | `SYNTHETIC_REPLICATIONS=1500 SYNTHETIC_POWER_REPLICATIONS=400 SYNTHETIC_SIMULATIONS=2000 ./.venv/bin/python Code/synthetic_timing_experiments.py` | `Data_Clean/synthetic_null_validation_summary.csv`, `Charts/synthetic_type1_calibration.png`, `Charts/synthetic_power_curve.png` |
| **Table S2, Figs S3–S4** — real-path positive control (injected skill α) | `Code/positive_control_power.py` | `PC_SEEDS=100 ./.venv/bin/python Code/positive_control_power.py` | `Data_Clean/GC=F_positive_control_power_summary.csv`, `Charts/positive_control_skill_curve.png`, `Charts/positive_control_example_histogram.png` |
| same, with stochastic execution costs (robustness) | `Code/positive_control_power.py` | `MONTE_CARLO_SIMULATE_EXECUTION_COSTS=1 PC_SEEDS=20 ./.venv/bin/python Code/positive_control_power.py` | `Data_Clean/GC=F_positive_control_power_costson_summary.csv` |
| **Table S3, Fig S5** — realistic-signal detection across signal-generating processes | `Code/positive_control_signals.py` | `./.venv/bin/python Code/positive_control_signals.py` | `Charts/positive_control_signal_curve.png` |
| **Table S4, Fig S6** — cross-asset panel, 322 tests, BH & Bonferroni control | `Code/cross_asset_multiplicity.py` | `./.venv/bin/python Code/cross_asset_multiplicity.py` | `Data_Clean/cross_asset_panel_summary.csv`, `Charts/cross_asset_multiplicity.png` |
| **Table S6, Fig S8** — alternative-null benchmark comparison (shuffled / stationary-bootstrap returns) | `Code/null_benchmark_comparison.py` | `./.venv/bin/python Code/null_benchmark_comparison.py` | `Charts/null_method_comparison.png` |
| **Table S7, Fig S9** — exploratory multi-asset walk-forward panel | `Code/multi_asset_walk_forward.py` | `./.venv/bin/python Code/multi_asset_walk_forward.py` | walk-forward CSVs in `Data_Clean/`, `Charts/multi_asset_walk_forward_inference.png` |
| **Table S8, Fig S10** — bounded frequency-robustness (daily / 4h / hourly) | `Code/frequency_robustness_summary.py` | `./.venv/bin/python Code/frequency_robustness_summary.py` | `Charts/frequency_robustness_pvalue_percentile.png` |
| **Tables S5, S9, S10 + Figs S11–S17** — per-strategy MC summaries, regime heatmap, RCSI/percentile/p-value-by-strategy, equity curves, seed-robustness | `Code/regenerate_gallery.py` | `./.venv/bin/python Code/regenerate_gallery.py` | gallery PNGs in `Charts/`; recomputed and verified against the published values before any figure is written |

Manuscript PDFs are assembled from `fortuna_full_backup.tex` and compiled with a
standard `elsarticle` LaTeX toolchain:

```bash
./.venv/bin/python Code/build_main.py        # writes paper.tex
./.venv/bin/python Code/build_supplement.py  # writes supplement.tex
latexmk -pdf paper.tex && latexmk -pdf supplement.tex
```

## Data provenance and terms

The underlying price series — gold futures (`GC=F`) and the cross-asset universe
of 47 instruments — were obtained from **Yahoo Finance** through the
[`yfinance`](https://github.com/ranaroussi/yfinance) package. They are
redistributed here **only in derived form** — regime-labeled OHLCV series,
realized trade logs, and per-asset result tables in `Data_Clean/` — which is what
the reproduction commands consume.

Raw vendor feeds are **not** redistributed: they remain subject to
[Yahoo Finance's terms of use](https://policies.yahoo.com/us/en/yahoo/terms/index.htm)
and to `yfinance`'s usage terms. To re-download raw data, set the ticker via the
`TICKER` environment variable and use the included loader:

```bash
TICKER="GC=F" ./.venv/bin/python Code/data_loader.py
```

`data_loader.py` pulls daily bars with `period="max"`, hourly bars with
`interval="1h"`, and resamples 1h→4h (Yahoo has no native 4h interval). Yahoo
data are provided for personal, non-commercial use; users are responsible for
compliance with the vendor's current terms.

## License

Code in this repository is released under the **MIT License** (see `LICENSE`).
The derived data in `Data_Clean/` is provided for the sole purpose of
reproducing the paper's results; redistribution of the underlying market data
remains governed by Yahoo Finance's terms of use.

## Citation

See `CITATION.cff`, or cite the archived release at
https://doi.org/10.5281/zenodo.20695539.
```

---

## (c) requirements.txt

Imports inspected across `Code/*.py` and `main.py`: only `numpy`, `pandas`, `matplotlib`, and `yfinance` are third-party (everything else is the standard library — `os`, `pathlib`, `dataclasses`, `json`, `math`, `hashlib`, etc.). **No `scipy`, `statsmodels`, `scikit-learn`, or `seaborn` is imported** — the bootstrap, KS test, and BH/Bonferroni control are implemented directly on top of NumPy. The existing `requirements.txt` is missing `numpy`, which 36 modules import; pin it.

```
# Reproducibility environment for Fortuna
# Python 3.11+ (developed under 3.14). Standard library covers everything else
# (os, pathlib, json, dataclasses, math, hashlib, random, ...).

numpy>=2.0,<3.0
pandas>=2.2,<3.0
matplotlib>=3.9,<4.0

# Only needed to re-download raw price data with Code/data_loader.py.
# Not required to reproduce the published numbers from Data_Clean/.
yfinance>=0.2,<1.0
```

> Conservative major-version caps as requested. The repo's current pins (`pandas==2.3.3`, `matplotlib==3.10.8`) sit inside these ranges; the `yfinance` entry was `1.2.0`, which is not a valid PyPI version — the published series are `0.2.x` — so it is corrected to a `>=0.2,<1.0` range. If you prefer exact pins for an archival freeze, use `numpy==2.3.x`, `pandas==2.3.3`, `matplotlib==3.10.8`, `yfinance==0.2.x` against the actual `pip freeze` of the run environment.

---

## (d) CITATION.cff

```yaml
cff-version: 1.2.0
message: "If you use this software or data, please cite both the article and the archived release."
title: "Fortuna: Structure-Preserving Randomization Inference for Decision Timing"
abstract: >-
  Replication code and derived data for a conditional randomization-inference
  framework for decision timing. The structure-preserving null holds the realized
  trade structure and price path fixed and re-randomizes only trade placement,
  yielding a finite-sample valid test, a measure-family sensitivity range with an
  intersection-union reading, a comparison against White's Reality Check and
  Hansen's SPA, and a 322-test cross-asset panel under multiplicity control.
type: software
authors:
  - family-names: Patel
    given-names: Aryan
repository-code: "https://github.com/ampatel355/FORTUNAFRAMEWORK"
url: "https://github.com/ampatel355/FORTUNAFRAMEWORK"
license: MIT
keywords:
  - conditional randomization inference
  - model risk
  - uncertainty quantification
  - Monte Carlo methods
  - sensitivity analysis
  - backtesting
  - data snooping
  - reproducibility
identifiers:
  - type: doi
    value: 10.5281/zenodo.20695539
    description: "Archived release on Zenodo"
preferred-citation:
  type: article
  title: "Structure-Preserving Randomization Inference for Decision Timing"
  authors:
    - family-names: Patel
      given-names: Aryan
  journal: "Risks"
  year: 2026
  publisher:
    name: "MDPI"
```

---

## Accuracy notes for the author

- **Git remote currently points to `VF-RCSI.git`, not `Fortuna`.** The paper, README, and `CITATION.cff` all advertise `github.com/ampatel355/FORTUNAFRAMEWORK`. Before release, either rename the GitHub repo or run `git remote set-url origin https://github.com/ampatel355/FORTUNAFRAMEWORK.git` so the public URL matches the manuscript's Data Availability Statement and the Zenodo archive.
- **No `LICENSE` or `CITATION.cff` exists yet** in the working tree — both are supplied above and need to be committed. `requirements.txt` exists but is incomplete (missing `numpy`; invalid `yfinance==1.2.0`) — replace with the block above.
- Table/figure *numbers* in the reproduction table follow the `tab:`/`fig:` label ordering in `paper.tex` and `supplement.tex`; the **script, env-var, and output-CSV names are exact** (taken from the scripts and the supplement's reproduction appendix, lines 663–701). The Table-3/Table-S* numbering should be sanity-checked against the final compiled PDFs since float numbering depends on placement.
- Relevant source files inspected: `/Users/aryanpatel/Desktop/Fortuna/supplement.tex` (§Reproducibility, lines 655–705), `/Users/aryanpatel/Desktop/Fortuna/paper.tex` (captions/labels), `/Users/aryanpatel/Desktop/Fortuna/Code/schedule_measure_sensitivity.py`, `/Users/aryanpatel/Desktop/Fortuna/Code/data_loader.py`, `/Users/aryanpatel/Desktop/Fortuna/requirements.txt`.